import { StyleSheet, StatusBar } from 'react-native';

const styles = StyleSheet.create({
    name: {
        fontSize: 20,
        fontWeight: '400'
    },
    desc: {
        fontSize: 18,
        fontWeight: '300',
    },
    loc: {
        flexDirection: 'row'
    }
});

export default styles;